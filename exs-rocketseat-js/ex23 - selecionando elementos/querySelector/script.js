// querySelector() - pega apenas o primeiro que ele achar - exs: meta (nome elemento), .one (classe igual css), [src] (source que liga o html com outros arquivos)

const element = document.querySelector('[src]');
console.log(element);